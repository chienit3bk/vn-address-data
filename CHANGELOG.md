# Changelog

## 1.0.0 — Unreleased

- Dataset ban đầu: 34 tỉnh/thành, 3.317 xã/phường (cơ cấu mới), 691 quận/huyện cũ. Nguồn: Nhất Tín Express.
- Thư viện TypeScript: `types`, `format` (slugify/cleanText/detectUnit/buildFullName), `data` (load* từ JSON), `dropdown` (2 dropdown Tỉnh → Phường/Xã).
- Bổ sung setup cho local dev + docs: `.nvmrc`, `LICENSE`, `engines`, `exports` map, script `prepare`/`dev`/`check`, đồng bộ lại `yarn.lock`, viết lại README.
